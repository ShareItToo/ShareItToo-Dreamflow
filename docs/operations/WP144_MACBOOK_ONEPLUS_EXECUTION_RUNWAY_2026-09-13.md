# WP144 — MacBook OnePlus execution runway

Status: **PREPARED ON MACBOOK; PHYSICAL ONEPLUS EXECUTION PENDING**.

## Result

WP143 is closed on exact GitHub head
`07ce24325648ed797893ac39a6fec6472273135e`: Regression
`34776401000`, its independent clean-checkout job, CodeQL `34776400991`
and zero open code-scanning alerts pass. PR #7 remains Draft, open,
mergeable and unmerged.

The MacBook remote connection is online, but ADB currently reports no Android
device. Its existing ShareItToo checkout is an old, locally changed checkout
on another remote and was deliberately left untouched. A separate clean
checkout of the official repository now exists on the exact WP143 branch and
head. The WP143 runner bytes match the Mac-mini source.

The exact four-file private candidate archive for signed Internal/Staging
`1.0.0+2026091312` was transferred byte-for-byte through the existing encrypted
tailnet. The MacBook independently validates application ID, version, source
commit, APK/AAB hashes, canonical signing certificate, Firebase configuration,
Google-only social profile and the finding-free binary privacy report. The
archive and its directory are owner-only.

The newest structurally eligible, fully retired synthetic owner/renter source
was also copied byte-for-byte and validated on the MacBook. Only its safe
lifecycle metadata was observed: two distinct roles, prior journey retired,
listing ended and no prior booking created. No address, password, token,
account identifier, credential-derived digest or private path is recorded.

Both one-time transfer services were stopped and their temporary Mac-mini
directories removed. Drive, Play and every provider remain unchanged.

All three focused WP144 validator tests, the combined 14 WP143/WP144 checks
and the complete Mac-mini technical regression pass, including zero-diagnostic
Flutter analysis, the complete Flutter suite, Web/Wasm, loopback smoke and the
Android debug build. No reduced or timing-based substitute was used.

## Remaining physical gate

The OnePlus CPH2581 must be connected, unlocked and ADB-authorized on the
MacBook. The versioned WP143 runner will then fail closed unless the exact
device, candidate, source, push opt-in and execution gates are present. It may
perform only a missing install or an older matching-version data-preserving
replacement; uninstall and app-data reset remain forbidden.

Runtime login of the transferred synthetic roles is intentionally not claimed
until the physical journey. The complete owner publication, renter discovery,
non-binding request/acceptance, chat, transactional FCM, Account-A/B isolation,
cancellation, listing retirement and protected-owner restoration must pass
before `oneplus-cross-device-two-role` can move from **OPEN**.

The conservative portfolio remains **21 PASS / 4 PARTIAL / 7 OPEN**. No phone,
Staging business data, Production, Store, Firebase Console, Stripe, payment,
money, PR merge, personal identity, credential or secret state changed.
